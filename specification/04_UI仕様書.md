# UI仕様書

## 1. デザインシステム

### 1.1 カラーパレット

```kotlin
object AppColors {
    // 背景グラデーション
    val backgroundGradientStart = Color(0xFFBAE6FD)  // sky-200
    val backgroundGradientEnd = Color(0xFF38BDF8)    // sky-400
    
    // メインカラー
    val primary = Color(0xFF3B82F6)        // blue-500
    val primaryDark = Color(0xFF2563EB)    // blue-600
    
    // アクセントカラー
    val accent = Color(0xFFFACC15)         // yellow-400
    val accentDark = Color(0xFFEAB308)     // yellow-500
    
    // ステータスカラー
    val correct = Color(0xFF22C55E)        // green-500
    val correctBg = Color(0xFFDCFCE7)      // green-100
    val wrong = Color(0xFFEF4444)          // red-500
    val wrongBg = Color(0xFFFEE2E2)        // red-100
    
    // ヒント
    val hintBg = Color(0xFFFFF7ED)         // orange-50
    val hintBorder = Color(0xFFFED7AA)     // orange-200
    val hintText = Color(0xFFEA580C)       // orange-600
    
    // その他
    val white = Color(0xFFFFFFFF)
    val cardBg = Color(0xFFFFFFFF)
    val textPrimary = Color(0xFF1F2937)    // gray-800
    val textSecondary = Color(0xFF6B7280)  // gray-500
    val textMuted = Color(0xFF9CA3AF)      // gray-400
    val disabled = Color(0xFFE5E7EB)       // gray-200
    
    // 漢字表示
    val kanjiBoxBg = Color(0xFFFEF9C3)     // yellow-100
    val kanjiBoxBorder = Color(0xFFFACC15) // yellow-400
    val kanjiHighlight = Color(0xFFFEF08A) // yellow-200
}
```

### 1.2 タイポグラフィ

```kotlin
object AppTypography {
    // アプリタイトル
    val titleLarge = TextStyle(
        fontSize = 28.sp,
        fontWeight = FontWeight.Bold
    )
    
    // 画面タイトル
    val titleMedium = TextStyle(
        fontSize = 22.sp,
        fontWeight = FontWeight.Bold
    )
    
    // セクションタイトル
    val titleSmall = TextStyle(
        fontSize = 18.sp,
        fontWeight = FontWeight.Bold
    )
    
    // 漢字表示（超大）
    val kanjiDisplay = TextStyle(
        fontSize = 96.sp,
        fontWeight = FontWeight.Bold
    )
    
    // 選択肢
    val choiceText = TextStyle(
        fontSize = 24.sp,
        fontWeight = FontWeight.Bold
    )
    
    // 本文
    val bodyLarge = TextStyle(
        fontSize = 18.sp,
        fontWeight = FontWeight.Normal
    )
    
    // 本文（小）
    val bodyMedium = TextStyle(
        fontSize = 16.sp,
        fontWeight = FontWeight.Normal
    )
    
    // キャプション
    val caption = TextStyle(
        fontSize = 14.sp,
        fontWeight = FontWeight.Normal
    )
}
```

### 1.3 形状・余白

```kotlin
object AppShapes {
    // カード
    val cardRadius = 24.dp
    val cardRadiusFull = 9999.dp  // Pill shape
    
    // ボタン
    val buttonRadius = 12.dp
    val buttonRadiusFull = 9999.dp
    
    // 漢字ボックス
    val kanjiBoxRadius = 16.dp
}

object AppSpacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp
    val lg = 24.dp
    val xl = 32.dp
}
```

## 2. 画面別UI仕様

### 2.1 スタート画面 (StartScreen)

```
┌─────────────────────────────┐
│                         [⚙️]│  ← 設定ボタン（右上）
│                             │
│           🚃                │  ← 電車絵文字（大）
│                             │
│        でんしゃ             │  ← タイトル
│      かん字クイズ           │
│   小学1年生のかん字 80字    │  ← サブタイトル
│                             │
│ ┌─────────────────────────┐ │
│ │ 📝  つうじょうモード    │ │  ← モード選択ボタン1
│ │     すべてのかん字から  │ │
│ │     しゅつだい          │ │
│ └─────────────────────────┘ │
│                             │
│ ┌─────────────────────────┐ │
│ │ 💪  にがてかん字モード  │ │  ← モード選択ボタン2
│ │     にがてなかん字が    │ │     （苦手がない場合は無効化）
│ │     ○こ あります       │ │
│ └─────────────────────────┘ │
│                             │
│    ┌───────────────────┐    │
│    │ これまでの正かいりつ │    │  ← 学習履歴（ある場合のみ）
│    │       85%          │    │
│    │  (170 / 200もん)   │    │
│    └───────────────────┘    │
│                             │
└─────────────────────────────┘
```

**コンポーネント詳細:**

- 背景: 水色グラデーション（上から下）
- 設定ボタン: 白背景の丸ボタン、⚙️アイコン
- タイトル: 白、太字、ドロップシャドウ
- モード選択カード: 白背景、角丸24dp、影付き
  - 通常モード: 常に有効
  - 苦手モード: 苦手漢字がある場合はオレンジ背景、ない場合はグレーで無効化
- 学習履歴: 半透明白背景、小さめの角丸カード

### 2.2 設定画面 (SettingsScreen)

```
┌─────────────────────────────┐
│ [←]  せってい               │  ← ヘッダー
│                             │
│ ┌─────────────────────────┐ │
│ │ もんだいすう             │ │  ← 問題数設定
│ │                         │ │
│ │ [10もん] [20もん] [ぜんぶ]│ │  ← 3択ボタン
│ └─────────────────────────┘ │
│                             │
│ ┌─────────────────────────┐ │
│ │ 📊 がくしゅうレポート  →│ │  ← レポート遷移ボタン
│ └─────────────────────────┘ │
│                             │
│ ┌─────────────────────────┐ │
│ │ 🗑️ きろくをリセット      │ │  ← リセットボタン（赤系）
│ │   すべてのきろくをけします │ │
│ └─────────────────────────┘ │
│                             │
└─────────────────────────────┘
```

**コンポーネント詳細:**

- 問題数選択: 3つのトグルボタン（選択中は青、非選択はグレー）
- レポートボタン: 白背景カード、右矢印付き
- リセットボタン: 薄い赤背景、赤文字

### 2.3 学習レポート画面 (ReportScreen)

```
┌─────────────────────────────┐
│ [←]  がくしゅうレポート     │  ← ヘッダー
│                             │
│ ┌─────────────────────────┐ │
│ │ ぜんたいのせいせき        │ │
│ │                         │ │
│ │  200      185      92%  │ │
│ │ といた   せいかい  正かい│ │
│ │ もんだい  すう    りつ   │ │
│ └─────────────────────────┘ │
│                             │
│ ┌─────────────────────────┐ │
│ │ にがてなかん字 (5こ)     │ │
│ │                         │ │
│ │ ┌─────────────────────┐ │ │
│ │ │ 森 (もり)    ✗3 / ○1│ │ │  ← 苦手漢字リスト
│ │ └─────────────────────┘ │ │
│ │ ┌─────────────────────┐ │ │
│ │ │ 竹 (たけ)    ✗2 / ○0│ │ │
│ │ └─────────────────────┘ │ │
│ │         ...             │ │
│ └─────────────────────────┘ │
│                             │
└─────────────────────────────┘
```

**コンポーネント詳細:**

- 全体統計: 3カラムレイアウト、数字は大きく太字
- 苦手漢字リスト: スクロール可能なリスト
  - 各行: 漢字（大）、読み（括弧内）、間違い/正解回数
  - オレンジ背景のカード
  - 苦手がない場合は「にがてなかん字はありません 🎉」を表示

### 2.4 クイズ画面 (QuizScreen)

```
┌─────────────────────────────┐
│ 🚃 でんしゃかん字  3/10 [×]│  ← ヘッダー
│ ████████░░░░░░░░░░░░░░░░░░ │  ← 進捗バー
│        ⭐ スコア: 2         │  ← スコア
│                             │
│ ┌─────────────────────────┐ │
│ │ 🚃 このかん字はなんとよむ？│ │  ← 青いヘッダー
│ │                         │ │
│ │      ╔═══════════╗      │ │
│ │      ║           ║      │ │
│ │      ║     森    ║      │ │  ← 漢字（超大、黄色枠）
│ │      ║           ║      │ │
│ │      ╚═══════════╝      │ │
│ │                         │ │
│ │   [💡 れいぶんを見る]    │ │  ← ヒントボタン（回答前のみ）
│ │                         │ │
│ │  ┌────────┐ ┌────────┐  │ │
│ │  │  もり  │ │  はな  │  │ │  ← 選択肢（2x2グリッド）
│ │  └────────┘ └────────┘  │ │
│ │  ┌────────┐ ┌────────┐  │ │
│ │  │  はやし │ │  くさ  │  │ │
│ │  └────────┘ └────────┘  │ │
│ │                         │ │
│ └─────────────────────────┘ │
│                             │
└─────────────────────────────┘
```

**回答後の状態:**

```
│ │      ╔═══════════╗      │ │
│ │      ║     森    ║      │ │
│ │      ╚═══════════╝      │ │
│ │                         │ │
│ │  ┌────────┐ ┌────────┐  │ │
│ │  │✓ もり  │ │  はな  │  │ │  ← 正解は緑、不正解は赤
│ │  └────────┘ └────────┘  │ │   他はグレーアウト
│ │  ┌────────┐ ┌────────┐  │ │
│ │  │  はやし │ │  くさ  │  │ │
│ │  └────────┘ └────────┘  │ │
│ │                         │ │
│ │ ┌───────────────────────┐│ │
│ │ │    ⭕ せいかい！      ││ │  ← 結果表示
│ │ │                       ││ │
│ │ │ 📖 つかいかた         ││ │
│ │ │ 「森」の中をはしる    ││ │  ← 例文（漢字ハイライト）
│ │ └───────────────────────┘│ │
│ │                         │ │
│ │ [      ➡️ つぎへ      ] │ │  ← 次へボタン
│ └─────────────────────────┘ │
```

**コンポーネント詳細:**

- ヘッダー: 白い角丸バー、アプリ名・進捗・終了ボタン
- 進捗バー: グレー背景、緑で塗りつぶし
- スコア: 白文字、⭐アイコン付き
- カード: 白背景、角丸24dp
  - 上部: 青い帯、電車絵文字とタイトル
  - 漢字ボックス: 黄色背景、黄色太枠、超大フォント
  - ヒントボタン: オレンジ系、枠線あり
  - 選択肢: 黄色ボタン、2x2グリッド
- 結果表示
  - 正解時: 緑背景、⭕
  - 不正解時: 赤背景、❌、正しい読みを表示
  - 例文: 白背景の内部カード、漢字部分は黄色ハイライト
- 次へボタン: 青、全幅

### 2.5 結果画面 (ResultScreen)

```
┌─────────────────────────────┐
│                             │
│           🚃                │
│                             │
│     クイズしゅうりょう！    │
│                             │
│          8 / 10             │  ← スコア（大きく）
│                             │
│     よくできました！👏      │  ← メッセージ
│                             │
│ ┌─────────────────────────┐ │
│ │ まちがえたかん字          │ │  ← 間違えた漢字（ある場合）
│ │ [森] [竹]                │ │
│ └─────────────────────────┘ │
│                             │
│ [   🔄 もういっかい     ]  │  ← 再挑戦ボタン（緑）
│                             │
│ [   🏠 スタートにもどる  ]  │  ← 戻るボタン（グレー）
│                             │
└─────────────────────────────┘
```

**スコア別メッセージ:**

| 条件 | メッセージ |
|------|-----------|
| 全問正解 | パーフェクト！すごい！🎉 |
| 80%以上 | よくできました！👏 |
| 60%以上 | がんばったね！💪 |
| 60%未満 | もういっかいチャレンジ！🔄 |

## 3. アニメーション

### 3.1 画面遷移
- フェードイン/アウト（300ms）

### 3.2 ボタン
- タップ時: scale 0.95（100ms）
- ホバー時: scale 1.05（100ms）

### 3.3 正解/不正解
- 正解ボタン: scale 1.05 + 緑に変化
- 不正解ボタン: 赤に変化（スケール変化なし）

### 3.4 進捗バー
- 幅の変化: 300ms ease

## 4. アクセシビリティ

- タップターゲット: 最小48dp
- コントラスト比: WCAG AA準拠
- フォントサイズ: システム設定に追従可能
- contentDescription: すべてのアイコンに設定

## 5. Jetpack Compose 実装例

### 背景グラデーション

```kotlin
@Composable
fun GradientBackground(
    content: @Composable () -> Unit
) {
    Box(
        modifier = Modifier
            .fillMaxSize()
            .background(
                brush = Brush.verticalGradient(
                    colors = listOf(
                        AppColors.backgroundGradientStart,
                        AppColors.backgroundGradientEnd
                    )
                )
            )
    ) {
        content()
    }
}
```

### 漢字表示ボックス

```kotlin
@Composable
fun KanjiBox(kanji: String) {
    Box(
        modifier = Modifier
            .background(
                color = AppColors.kanjiBoxBg,
                shape = RoundedCornerShape(AppShapes.kanjiBoxRadius)
            )
            .border(
                width = 4.dp,
                color = AppColors.kanjiBoxBorder,
                shape = RoundedCornerShape(AppShapes.kanjiBoxRadius)
            )
            .padding(AppSpacing.lg),
        contentAlignment = Alignment.Center
    ) {
        Text(
            text = kanji,
            style = AppTypography.kanjiDisplay,
            color = AppColors.textPrimary
        )
    }
}
```

### 選択肢ボタン

```kotlin
@Composable
fun ChoiceButton(
    text: String,
    state: ChoiceState,  // DEFAULT, CORRECT, WRONG, DISABLED
    onClick: () -> Unit
) {
    val backgroundColor = when (state) {
        ChoiceState.DEFAULT -> AppColors.accent
        ChoiceState.CORRECT -> AppColors.correct
        ChoiceState.WRONG -> AppColors.wrong
        ChoiceState.DISABLED -> AppColors.disabled
    }
    
    val textColor = when (state) {
        ChoiceState.DEFAULT -> AppColors.textPrimary
        ChoiceState.CORRECT, ChoiceState.WRONG -> AppColors.white
        ChoiceState.DISABLED -> AppColors.textMuted
    }
    
    val scale by animateFloatAsState(
        targetValue = if (state == ChoiceState.CORRECT) 1.05f else 1f
    )
    
    Button(
        onClick = onClick,
        enabled = state == ChoiceState.DEFAULT,
        modifier = Modifier
            .fillMaxWidth()
            .scale(scale),
        colors = ButtonDefaults.buttonColors(
            containerColor = backgroundColor,
            disabledContainerColor = backgroundColor
        ),
        shape = RoundedCornerShape(AppShapes.buttonRadius)
    ) {
        Text(
            text = text,
            style = AppTypography.choiceText,
            color = textColor,
            modifier = Modifier.padding(vertical = AppSpacing.md)
        )
    }
}
```

### 例文のハイライト表示

```kotlin
@Composable
fun SentenceWithHighlight(
    sentence: String,
    highlightKanji: String
) {
    val parts = sentence.split(highlightKanji)
    
    Text(
        buildAnnotatedString {
            parts.forEachIndexed { index, part ->
                append(part)
                if (index < parts.size - 1) {
                    withStyle(
                        SpanStyle(
                            fontWeight = FontWeight.Bold,
                            color = AppColors.primary,
                            background = AppColors.kanjiHighlight
                        )
                    ) {
                        append(highlightKanji)
                    }
                }
            }
        },
        style = AppTypography.bodyLarge
    )
}
```
